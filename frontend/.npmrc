# npm 配置文件
# 提升安装速度和稳定性

# 使用国内镜像（可选，如果默认源速度慢）
# registry=https://registry.npmmirror.com

# 严格的 peer dependencies 检查
legacy-peer-deps=false

# 自动安装 peer dependencies（npm 7+ 默认行为，无需配置）

# 保存精确版本
save-exact=false

